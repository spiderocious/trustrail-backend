# Server Configuration
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/trustrail

# JWT Secrets (use strong 256-bit random strings)
JWT_SECRET=your-jwt-secret-256-bit-minimum-change-this-in-production
ADMIN_JWT_SECRET=your-admin-jwt-secret-256-bit-different-from-business-secret

# PWA API Integration
PWA_BASE_URL=https://api.dev.onepipe.io/v2/transact
PWA_API_KEY=your-pwa-api-key-from-paywithaccount
PWA_CLIENT_SECRET=your-pwa-client-secret-from-paywithaccount

# Admin Credentials (hardcoded for MVP)
ADMIN_EMAIL=admin@trustrail.com
ADMIN_PASSWORD=secure-admin-password-change-this

# Background Job Intervals (milliseconds)
STATEMENT_ANALYSIS_JOB_INTERVAL=60000
PAYMENT_MONITOR_JOB_INTERVAL=300000

# PWA Mock Mode (Inspect for testing, Live for production)
PWA_MOCK_MODE=Inspect

# OpenAI Integration (for bank statement analysis)
OPENAI_ENABLED=true
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o
OPENAI_PROMPT="Analyze this bank statement (PDF or CSV) and calculate a trust score (0-100). Return structured JSON with: trustScore, decision, periodCovered, incomeAnalysis, spendingAnalysis, balanceAnalysis, behaviorAnalysis, debtProfile, affordabilityAssessment, riskFlags, and ruleCompliance."
